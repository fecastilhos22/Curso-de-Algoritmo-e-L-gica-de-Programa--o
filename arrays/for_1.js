const numeros = [1, 2, 356, 4, 5, 6, 11];

for (let i = 0; i < numeros.length; i++){ //para não fixar o valor do array a percorrer, para percorrer todo ele
    console.log(numeros[i]);
}

//no for tu controla o indice
